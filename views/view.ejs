
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link  rel="stylesheet" href="/styles/view.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Khojki&family=Noto+Sans+Tangsa:wght@400..700&family=Roboto+Slab:wght@100..900&display=swap" rel="stylesheet">
    <title>View</title>
</head>
<body>
  <%- include("partials/header.ejs") %>

  <% if (blogs.length === 0) {%>
    <h1 class="title">You have not create any posts &#128565;</h1>
    <a href="/create" id="link"><h2 class="link">Create your first blog today!</h2></a>
  <% } else { %>
    <% blogs.forEach((post) => {%>
        <div class="row mb-2 position-relative py-2 px-4">
            <div class="col-md-12">
                <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative"  id="card">
                  <div class="col p-4 d-flex flex-column position-static">
                    <h3 class="mb-0" style="color:#776B5D; font-size: 40px; font-family: Noto Sans Tangsa, sans-serif"><%= post.title %></h3>
                    <p class="card-text mb-auto" style="padding-top: 10px; font-size:25px; color:#776B5D"><%= post.content.substring(0, 100) %></p>
                    <a href="/view/<%= post.id %>" class="icon-link gap-1 icon-link-hover stretched-link" style="color: #DED0B6">
                      Continue reading
                    </a>
                  </div>
                </div>
              </div>
        </div>
    <% }) %> 
  <% } %> 
  <%- include("partials/footer.ejs") %>
</body>
</html>

